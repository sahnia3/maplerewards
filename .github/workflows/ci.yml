name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  backend:
    name: backend (go ${{ matrix.go }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go: ['1.25']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          cache: true

      - name: go vet
        run: go vet ./...

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: v1.59
          args: --timeout=5m

      - name: go build
        run: go build -v ./...

      - name: go test (race detector)
        run: go test -race -count=1 ./...

      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          $(go env GOPATH)/bin/govulncheck ./...

  docker:
    name: docker build verification
    runs-on: ubuntu-latest
    needs: backend
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: build image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: maplerewards-api:ci

      - name: smoke check image (binary exists, no root)
        run: |
          docker image inspect maplerewards-api:ci > /dev/null
          # Confirm the image runs as a non-root user (matches Dockerfile USER 10001).
          USER_FIELD=$(docker image inspect -f '{{.Config.User}}' maplerewards-api:ci)
          if [ "$USER_FIELD" != "10001:10001" ]; then
            echo "Dockerfile must drop to USER 10001:10001 — got '$USER_FIELD'"
            exit 1
          fi

  frontend:
    name: frontend (node ${{ matrix.node }})
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    strategy:
      matrix:
        node: ['20']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: install
        run: npm ci

      - name: typecheck
        run: npx tsc --noEmit

      - name: lint
        run: npm run lint

      - name: build (production)
        run: npm run build

      - name: npm audit (production deps, high+)
        run: npm audit --omit=dev --audit-level=high
