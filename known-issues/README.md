# Known Issues

Document bugs, gotchas, and resolutions here so Claude doesn't re-diagnose them.

Use `/log-issue <description>` to add a new entry, or create a markdown file manually.

## Format

```markdown
# Issue Title
## Symptoms — what fails
## Root Cause — why
## Fix — how it was resolved
## Date — when
```
